(* 03 — Test module for functional operations *)

module Ops = Functional_ops

let sum_list = Ops.sum_list
let product_list = Ops.product_list
let all_positive = Ops.all_positive
let find_even = Ops.find_even
let count_evens = Ops.count_evens
let reverse_list = Ops.reverse_list
let apply_n = Ops.apply_n
let compose = Ops.compose
