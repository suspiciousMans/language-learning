[package]
name = "rust-08-ecosystem"
version = "0.1.0"
edition = "2021"

[dependencies]
# Learner: fill in the versions yourself.
#
# serde = { version = "1.0", features = ["derive"] }
# serde_json = "1.0"
#
# Use `cargo add serde serde_json` to add them with the right version,
# or copy the lines above and run `cargo update`.
