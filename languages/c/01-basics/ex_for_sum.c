#include <stdio.h>

int main(void) {
    /* Sum the integers from 1 to N, inclusive.
     * N is hardcoded for now; later you can read it from the user.
     */

    int N = 100;
    long sum = 0;

    /* TODO: write a for loop that sums 1..N and stores in 'sum' */

    printf("Sum of 1..%d = %ld\n", N, sum);
    return 0;
}
