#include <stdio.h>

/* Return the larger of two integers. */
int max_of(int a, int b) {
    /* TODO: implement */
    return 0;
}

int main(void) {
    printf("max_of(3, 7) = %d\n", max_of(3, 7));
    printf("max_of(-1, -5) = %d\n", max_of(-1, -5));
    printf("max_of(4, 4) = %d\n", max_of(4, 4));
    return 0;
}
