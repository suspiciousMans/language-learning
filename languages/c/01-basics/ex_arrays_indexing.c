#include <stdio.h>

int main(void) {
    /* Declare an array of 10 ints, fill 0..9, print each, and sum them. */

    int arr[10];
    long sum = 0;

    /* TODO: fill arr with 0..9 using a loop */

    printf("Array contents:\n");
    for (int i = 0; i < 10; i++) {
        /* TODO: print arr[i] */
    }

    /* TODO: compute sum in a loop */

    printf("Sum: %ld\n", sum);
    return 0;
}
